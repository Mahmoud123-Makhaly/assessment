1-to install node-modules
yarn

2-The app uses a local mock API (data/db.json) powered by json-server.
Run the following command in a new terminal:
npx json-server --watch data/db.json --port 4000

3-to run project
yarn dev
